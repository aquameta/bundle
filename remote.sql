------------------------------------------------------------------------------
-- REMOTE REPOSITORIES
------------------------------------------------------------------------------

/*
create table remote (
    id uuid not null default public.uuid_generate_v4() primary key,
    remote_id uuid references remote.remote(id)
);
*/

